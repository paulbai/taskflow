"use client";

// Placeholder — the task manager upgrade lands in Phase 6.
export function TasksRoute() {
    return (
        <div style={{ textAlign: 'center', padding: '80px 24px', color: 'var(--text-tertiary)' }}>
            <div style={{ fontSize: 40, marginBottom: 12 }}>✅</div>
            <h2 style={{ color: 'var(--text-primary)', marginBottom: 8 }}>Task board coming online…</h2>
            <p>The upgraded task manager is being built.</p>
        </div>
    );
}
