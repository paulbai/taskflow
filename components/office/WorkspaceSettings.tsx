"use client";

// Placeholder — full settings land in Phase 11.
export function WorkspaceSettings() {
    return (
        <div style={{ textAlign: 'center', padding: '80px 24px', color: 'var(--text-tertiary)' }}>
            <div style={{ fontSize: 40, marginBottom: 12 }}>⚙️</div>
            <h2 style={{ color: 'var(--text-primary)', marginBottom: 8 }}>Workspace settings coming online…</h2>
            <p>Member management and workspace options are being built.</p>
        </div>
    );
}
