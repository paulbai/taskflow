"use client";

// Placeholder — the full block editor lands in Phase 4.
export function PageEditorRoute() {
    return (
        <div style={{ textAlign: 'center', padding: '80px 24px', color: 'var(--text-tertiary)' }}>
            <div style={{ fontSize: 40, marginBottom: 12 }}>📝</div>
            <h2 style={{ color: 'var(--text-primary)', marginBottom: 8 }}>Page editor coming online…</h2>
            <p>The block editor is being built. Check back shortly.</p>
        </div>
    );
}
